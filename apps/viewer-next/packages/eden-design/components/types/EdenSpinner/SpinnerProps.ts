export type SpinnerProps = {
  size?: string | number;
  baseColor?: string;
  primaryColor?: string;
  secondaryColor?: string;
  strokeWidth?: string | number;

  /* 预设颜色 */
  brand?: boolean;
  white?: boolean;
};